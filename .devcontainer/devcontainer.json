{
    "name": "HSR Scene Graph Dev Container",
    "privileged": true,
    "remoteUser": "hsr",
    "build": {
        "dockerfile": "Dockerfile",
        "args": {
            "USERNAME": "hsr",
            "DOCKER_REPO": "osrf/ros",
            "ROS_DISTRO": "humble",
            "IMAGE_SUFFIX": "-desktop-full"
        },
        "options": ["--build-arg", "BUILDKIT_INLINE_CACHE=1"]
    },
    "workspaceFolder": "/home/ws",
    "workspaceMount": "source=${localWorkspaceFolder},target=/home/ws,type=bind",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-vscode.cpptools",
                "ms-vscode.cpptools-themes",
                "twxs.cmake",
                "donjayamanne.python-extension-pack",
                "eamodio.gitlens",
                "ms-iot.vscode-ros",
                "ms-python.black-formatter",
                "anthropic.claude-code",
                "openai.chatgpt"
            ]
        }
    },
    "containerEnv": {
        "ROS_LOCALHOST_ONLY": "0",
        "ROS_DOMAIN_ID": "25",
        "NVIDIA_DRIVER_CAPABILITIES": "all",
        // Domain 5 matches the robot. vision-transport carries images and
        // point clouds over Zenoh, so heavy data does not cross as raw DDS.
        "RMW_IMPLEMENTATION": "rmw_cyclonedds_cpp",
        // tmc_gazebo_worlds' env hook only sets GAZEBO_MODEL_PATH (Gazebo
        // Classic); Ignition reads these, and without them every model:// URI
        // in the world fails to resolve
        "GZ_SIM_RESOURCE_PATH": "/home/ws/ros2_ws/install/tmc_gazebo_worlds/share/tmc_gazebo_worlds/models",
        "IGN_GAZEBO_RESOURCE_PATH": "/home/ws/ros2_ws/install/tmc_gazebo_worlds/share/tmc_gazebo_worlds/models"
    },
    "hostRequirements": {
        "gpu": "optional"
    },
    "runArgs": [
        "--net=host",
        "-e", "DISPLAY=${localEnv:DISPLAY}",
        "--ulimit", "memlock=-1:-1",
        "--ulimit", "rtprio=99",
        "--shm-size=8g"
    ],
    "mounts": [
        "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind,consistency=cached",
        "source=/dev/dri,target=/dev/dri,type=bind,consistency=cached"
    ],
    "initializeCommand": "sh ./.devcontainer/initialize.sh",
    "postCreateCommand": "bash ./.devcontainer/postCreate.sh"
}
